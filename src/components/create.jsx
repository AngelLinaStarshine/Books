import React from 'react';
import Form from './form';

const Create = ({ books }) => {
    return (
        <div>
            <h1>Create</h1>
            <Form />
        </div>
    )
};

export default Create;